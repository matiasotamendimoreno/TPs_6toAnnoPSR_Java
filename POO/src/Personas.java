 
// import java.util.Scanner;

public class Personas {
	// Scanner scanner = new Scanner(System.in);
	String name; // Es un atributo tipo String
	int edad; // Es un atributo tipo integer
	
	public void Saludar(){
		/* 
		 * name = System.in.toString(System.out.println("+ Ingresa tu nombre: "));
		 * System.out.print("+ Ingrese el nombre: ");
		 * name = scanner.nextLine();
		 * System.out.print("+ Ingrese su edad: ");
		 * edad = scanner.nextInt();
		 * */
		
		System.out.println("- Buenos días " + name + ". :D \n- Tu edad es " + edad + ". :O");
	}
}

/* 
 * Pregunta 1:
 * 		Una clase en Java es una plantilla o modelo a partir del cual se crean objetos y también define un
 * conjunto de propiedades (Atributos) y metodos (Comportamientos).
 * 
 * Pregunta 3:
 * 		Instanciar significa crear una instancia (Objeto) de una clase, esto se hace con la palabra clave
 * "new" seguido de sus nuevos atributos o metodos del objeto instanciado.
 * */
